## 1. Move the floor and clear what it reaches

- [x] 1.1 Set `minAppVersion` to `1.8.7` in `manifest.json`. Verify with `npm run lint`: `obsidianmd/no-unsupported-api` no longer reports a call at or below 1.8.7 (there are none yet) and still resolves the floor from the manifest
- [x] 1.2 In `src/GedcomView.ts`, have `leafShowing` iterate `iterateAllLeaves` and `openDocumentLink` call `void this.app.workspace.revealLeaf(open)` in place of `setActiveLeaf`; drop the comments that explain the root-only search and the declined API, and the matching sentence in the `leafShowingFile` doc comment in `src/vault/openTabs.ts`. Verify with `npm run typecheck` and the existing `src/vault/openTabs.test.ts` still passing; verify by hand in `demo-vault/` that a `FILE` link to an image open in a collapsed sidebar opens the sidebar and shows the tab, and that the GEDCOM tab keeps its file
- [x] 1.3 Rewrite "The minimum app version, and what it costs" in `CLAUDE.md`: the floor is 1.8.7, moved once for `getLanguage()`, the `revealLeaf` row removed, the `getSettingDefinitions` row kept with its 1.13.0 note; update the `minAppVersion` line in the `context` of `openspec/config.yaml` and the comment above `display()` in `src/settings.ts`. Verify with `grep -rn "1\.5\.0" CLAUDE.md openspec/config.yaml src` returning only `versions.json`-style history, if anything

## 2. The catalogue

- [x] 2.1 Create `src/i18n/en.json`, a flat object of dotted keys to messages, with every string the plugin shows today moved in verbatim: command names, notices, setting names, descriptions and dropdown options, status bar parts, search bar placeholders and tooltip labels, media popover rows and buttons, the record switcher placeholder, the rename dialog's title, field name and button, and the note-block line prefix. Plural messages (`problems`, `references`, `media links`, `files`) are tables with `one` and `other`. Verify in `src/i18n/i18n.test.ts` (task 2.4)
- [x] 2.2 Create `src/i18n/ru.json` with the Russian side, plural tables carrying `one`, `few`, `many` and `other`, wording aligned with Obsidian's own Russian where the concept has a name there; `GEDCOM`, key names and identifiers stay in Latin via placeholders. Include the six CodeMirror phrases under their own keys. Verify with `npm run typecheck`: removing any key from `ru.json` fails the build
- [x] 2.3 Create `src/i18n/index.ts`, which imports both JSON files and exports `MessageKey`, `setLanguage(code)`, `currentLanguage()`, `t(key, params?)` and `plural(key, count, params?)`: primary-subtag matching, English default, English fallback for a message absent in Russian, `{name}` interpolation, `Intl.PluralRules` selection with `other` as the fallback form. The module imports nothing from `obsidian`. Verify in new `src/i18n/i18n.test.ts`: `ru-RU` selects Russian and `de` selects English; interpolation; Russian problem counts for 1, 2, 5 and 21 pick singular, paucal, plural, singular; English picks `one` and `other`; both sides carry the same keys with the same placeholders; a test helper resets the language in `afterEach`
- [x] 2.4 In `src/i18n/i18n.test.ts`, hold every English string to sentence case by calling the evaluator behind `obsidianmd/ui/sentence-case` with the options `eslint.config.mjs` gives the source files, the JSON sibling rule being unable to parse a JSON file. Verify by spelling one entry `Go To Record` and watching the test fail

## 3. The plugin speaks through it

- [x] 3.1 In `src/main.ts`, call `setLanguage(getLanguage())` first in `onload`; resolve each command's `name` with `t` in `addCommand`; route the protocol-handler and link notices, the note-block problem line prefix, the record switcher's placeholder and the rename dialog's title, field name and button through `t`. In `src/commands.ts`, make `GedcomCommand.name` a `MessageKey` and route the copy-link and rename notices through `t`, the references notice through `plural`. Verify in `src/commands.test.ts`: names resolve to the English strings they were, and the references notice reads `1 GEDCOM reference` and `3 GEDCOM references` under English
- [x] 3.2 In `src/settingDefinitions.ts`, turn `SETTING_DEFINITIONS` and `RECORD_PREVIEW_OPTIONS` into functions that resolve names, descriptions and options with `t` when called; in `src/settings.ts`, call them from `getSettingDefinitions()` and `display()`. Verify in `src/settings.test.ts`: under English the definitions carry the strings they carried; under Russian, set with the i18n test helper, the first definition's name is Russian
- [x] 3.3 In `src/editor/status.ts`, route `formatVersion` and `formatProblems` through `t` and `plural`. Verify in `src/editor/status.test.ts`: the existing English cases unchanged; new cases under Russian for 0, 1, 2, 5 and 21 problems and for "not checked"
- [x] 3.4 In `src/editor/searchPanel.ts`, route the two placeholders, the five tooltip labels and the close button's label through `t`, keeping `spellKey` untouched. Verify in `src/editor/searchKeys.test.ts` or a new case in `src/editor/searchPanel.test.ts` (happy-dom) that under Russian the next button's `aria-label` is a Russian first line followed by `\nF3` on a non-Mac
- [x] 3.5 In `src/editor/mediaPreviewView.ts`, make `REMOTE_NOTES` and `OFFERS` tables of keys resolved at draw time, and route "File not found", "Image could not be loaded" and "Image could not be drawn" through `t`. Verify in `src/editor/mediaPreviewView.test.ts`: the existing English assertions unchanged; one new case under Russian for the remote-file row
- [x] 3.6 In `src/vault/renamedMedia.ts`, replace `count` with `plural` in `describeRetarget`, `describeUnreadable` and `describeStranded`; in `src/GedcomView.ts`, route the two notices through `t`. Verify in `src/vault/renamedMedia.test.ts`: `1 media link in 1 file`, `3 media links in 2 files` under English, and the Russian forms for 1, 3 and 5 links
- [x] 3.7 In `src/editor/hostExtensions.ts`, add `EditorState.phrases.of(...)` with the six CodeMirror phrases from the Russian side when `currentLanguage()` is `ru`, and nothing otherwise. Verify in a new case in `src/editor/service.test.ts` or `src/editor/hostExtensions.test.ts`: an `EditorState` built with the extensions answers `state.phrase("No diagnostics")` in Russian under Russian and unchanged under English

## 4. The harness and the browser

- [x] 4.1 In `harness/mount.ts`, accept an optional `language` in `HarnessOptions` and call `setLanguage` with it (English when absent) before building the state. Verify with `npm run build` of the harness and the existing browser specs unchanged
- [x] 4.2 Add a spec to `tests/search.spec.ts` that mounts with `language: "ru"`, opens the bar and asserts the find field's placeholder and the next button's `aria-label` are Russian, the label ending in `F3`; add a spec to `tests/mediaPreview.spec.ts` that mounts in Russian and asserts the missing-file row is Russian and still shows the target path; add a spec to `tests/panels.spec.ts` that opens the panel in Russian on a clean file and asserts its title and only line are Russian. Verify by running the three specs

## 5. Check

- [x] 5.1 Run `npm run check` and `npm run test:browser`, both green
