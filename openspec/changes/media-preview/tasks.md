## 1. The setting

- [x] 1.1 Add `mediaPreview: RecordPreviewTrigger` to `GedcomSettings` and `DEFAULT_SETTINGS` (`"modifier"`) in `src/settingsData.ts`, and read it in `parseSettings` with the existing `isRecordPreviewTrigger` guard — verify by extending `src/settings.test.ts`: a settings file without the key parses to the default, an invalid value falls back to the default, and `changedSetting("mediaPreview", "hover")` is accepted while a bad value returns `null`.
- [x] 1.2 Add the `Media preview` dropdown to `SETTING_DEFINITIONS` in `src/settingDefinitions.ts`, reusing `RECORD_PREVIEW_OPTIONS`, and verify in `src/settings.test.ts` that every definition's key exists in `DEFAULT_SETTINGS` and that record preview and media preview are separate entries.

## 2. Deciding what to draw

- [x] 2.1 Create `src/editor/media.ts` with `MediaPreviewContent` (the `image` / `file` / `remote` / `missing` union from design.md) and `mediaPreviewContent(reference, resolve)` — verify with a new `src/editor/media.test.ts` covering: an `http` target answering `remote` even when `mediaKind` is `image`; a relative path the resolver rejects answering `missing`; a path escaping the vault answering `missing`; `mediaKind: "image"` answering `image` with the resolved URL; and `audio`, `video`, `document`, `unknown` answering `file` with the file name.
- [x] 2.2 Carry `title` through every branch in `mediaPreviewContent`, absent where the reference has none, and verify in `src/editor/media.test.ts` that a reference without a title yields content with no title rather than the file name.
- [x] 2.3 Add `drawnCrop(crop, naturalWidth, naturalHeight)` to `src/editor/media.ts` returning the rectangle to draw or `null` for "show the whole image" — verify in `src/editor/media.test.ts`: a rectangle inside the image is returned unchanged; one overhanging an edge is clamped to the overlap; one entirely outside returns `null`; a zero width or height returns `null`.
- [x] 2.4 Add `previewBounds(paneWidth, paneHeight)` to `src/editor/media.ts` returning the max width and height for the popover — 60% and 40% of the pane, each capped at the px ceiling — verify in `src/editor/media.test.ts`: a wide pane is capped at the ceiling, a phone-width pane yields the fraction, and a zero or missing measurement yields the ceiling so the CSS fallback applies.

## 3. The hover extension

- [x] 3.1 Create `src/editor/mediaPreviewHover.ts` exporting `mediaPreviewHover({ language, trigger, delay, show, hide })`, resolving the offset under the pointer to a `Position` with `offsetToPosition` and querying `language.update(state.doc).getMediaAt(...)` — verify with a new `src/editor/mediaPreviewHover.test.ts` covering the show/hide transition against a fake service: a position answering media shows once and does not re-show while the pointer stays on it, a position answering `null` hides, and a `trigger` returning false never shows.
- [x] 3.2 Wire `mediaPreviewHover` into `createGedcomComposition` in `src/editor/composition.ts` beside `recordPreviewHover`, taking a second `PreviewGesture` and `showMedia`/`hideMedia` callbacks in `GedcomCompositionOptions`, passing `HOVER_TIME_MS` as the delay when the trigger is bare hover and `0` for the modifier — verify by extending `src/editor/previewGesture.test.ts` with the delay-selection helper, and by `npm run typecheck` passing with the harness updated in 5.1.
- [x] 3.3 Close an open media preview on modifier release, matching `closePreviewOnRelease` — verify in `src/editor/mediaPreviewHover.test.ts` that a keyup satisfying `gesture.closes` hides an open preview and does nothing when none is open.

## 4. Drawing it, and the arbitration

- [x] 4.1 Render the popover in `src/GedcomView.ts`: a `showMediaPreview` that builds a `HoverPopover` and switches on `MediaPreviewContent` — an `<img>` from `vault.getResourcePath` for `image`, a one-line row with icon, title and file name for `file`, a row naming the target as remote and unfetched for `remote`, and a not-found row for `missing` — verify by the Playwright specs in 5.2.
- [x] 4.2 Draw a rectangle as a container with `overflow: hidden` and an offset `<img>`, re-clamping with `drawnCrop` on the image's `load` event and falling back to the whole image where it returns `null` — verify by the Playwright specs in 5.3.
- [x] 4.3 Guard every handler that runs after the popover is built — the `load` clamp above included — against the popover no longer being the current one, by capturing it on show and returning early when it is not `this.mediaPreview` — verify by the Playwright spec in 5.6.
- [x] 4.4 Decline the record preview where the position also answers `getMediaAt`, by wrapping the `show` handed to `recordPreviewHover` in `createGedcomComposition` so the harness inherits the rule too — verified in `tests/mediaPreview.spec.ts` that the gesture over an `OBJE` pointer shows the image and records no record preview, while the gesture over a non-media XREF still shows the record.
- [x] 4.5 Set `--gedcom-media-max-w` and `--gedcom-media-max-h` on the popover from `previewBounds(contentEl.clientWidth, contentEl.clientHeight)` when showing it — verify by the Playwright spec in 5.4.
- [x] 4.6 Add `.gedcom-media-preview` to `styles.css` reading those properties with the window as fallback — `max-width: min(var(--gedcom-media-max-w, 60vw), 560px)`, `max-height: min(var(--gedcom-media-max-h, 40vh), 400px)`, `object-fit: contain` — plus the crop container and the caption row, all in Obsidian's own variables rather than fixed colours — verify by the Playwright spec in 5.4.

## 5. The harness and the browser specs

- [x] 5.1 Extend `harness/mount.ts` with a `mediaPreview` trigger option, a stub resolver mapping a vault path to a data URL, a `mediaPreviews` array on `HarnessCalls`, a pane width so a spec can make the pane narrower than the window, and a resolver mode whose image load the spec releases on demand — mounting the same `createGedcomComposition` — verify `npm run build` and the existing specs still pass.
- [x] 5.2 Add `tests/mediaPreview.spec.ts` covering the four branches against the harness: a `FILE` payload showing an image, an audio `FILE` showing the named row, an `http` payload showing the remote row with no network request recorded, and a missing path showing the not-found row.
- [x] 5.3 Extend `tests/mediaPreview.spec.ts` with the rectangle: a link whose `CROP` names a region shows that region, two links to one record show two different regions, the record's own `FILE` shows the whole image, and a rectangle past the edge shows the overlap rather than an empty box.
- [x] 5.4 Extend `tests/mediaPreview.spec.ts` with the bound: an image far larger than its pane renders within the box undistorted, a pane narrower than the window keeps the popover inside the pane, and the same preview at a phone-width viewport stays inside the window.
- [x] 5.5 Extend `tests/mediaPreview.spec.ts` with the setting: media preview off leaves record previews working, and the two triggers can differ so a bare hover shows a record while a media position waits for the modifier.
- [x] 5.6 Extend `tests/mediaPreview.spec.ts` with the staleness guard, using a harness image whose load the spec releases on demand: moving the pointer to a second media position before the first image loads leaves the second popover showing its own media, and closing the popover before the load leaves nothing drawn and no console error.

## 6. Documentation

- [x] 6.1 Add the `Media preview` setting to the README's settings list and state that a remote `FILE` is never fetched — verify the README's settings list names every entry in `SETTING_DEFINITIONS`.

## 8. Hover without a modifier, and a line that says it is live

- [x] 8.1 Default both `recordPreview` and `mediaPreview` to `"hover"` in `src/settingsData.ts` — verify in `src/settings.test.ts` that both defaults read `"hover"` and that a stored `"modifier"` still parses to `"modifier"`.
- [x] 8.2 Pass `delay: hoverDelay(trigger)` to `recordPreviewHover` as well as to `mediaPreviewHover` in `src/editor/composition.ts`, taking a `delay` for each in `GedcomCompositionOptions` — verify in `src/editor/mediaPreviewHover.test.ts` that `hoverDelay` answers `HOVER_TIME_MS` for hover and `0` for the modifier and for off, and by `npm run typecheck` with the harness updated.
- [x] 8.3 Create `src/editor/mediaSpans.ts` with `mediaSpans(state, language, ranges)` returning `{ from, to, media }` for every visible line that answers `getMediaAt`, `from` at the tag and `to` at the end of the line, prefiltered by a `FILE`/`OBJE` tag match — verify with a new `src/editor/mediaSpans.test.ts` covering: a `FILE` line answering with a span that starts at the tag and not at the level; an `OBJE` link line answering; a `FAMS` line skipped without the service being asked; and a line whose record names no file answering nothing.
- [x] 8.4 Dress every reference as the link it is: `tags.variableName` carries a `gedcom-reference-link` class in `src/editor/obsidianTheme.ts`, with the link colour, `--cursor-link` and the hover underline `.gedcom-internal-link` uses, and no weight of its own — verify in `tests/theme.spec.ts` that `@F1@` wears the link colour in both themes and takes an underline under the pointer. A media-only mark was written first and then removed: once a reference is a link, it said nothing the dressing did not already say.
- [x] 8.5 Keep a declaration apart from a reference to it: `tags.definition(tags.variableName)` carries a `gedcom-reference-declaration` class whose selector is doubled so it outweighs the link rule by specificity rather than by source order — verify in `tests/theme.spec.ts` that the declaring `@I1@` keeps the definition colour and semibold weight.
- [x] 8.6 Resolve the hover through the spans rather than by offset, so the gesture answers over the dressed extent, in `src/editor/mediaPreviewHover.ts` — verify in `src/editor/mediaPreviewHover.test.ts` that an offset inside a span answers, one on the level number does not, and the span's own line is what keys the session.
- [x] 8.7 Extend `tests/mediaPreview.spec.ts`: hovering the `FILE` tag shows the same popover as hovering the payload, hovering the level number shows none, an `OBJE` line carrying media is dressed while a `FAMS` line beside it is not, and a bare hover crossing a line without stopping opens nothing while a rest opens it.

- [x] 8.8 Gate the pointer cursor and the hover underline on the platform modifier, in a `src/editor/modifierHeld.ts` view plugin that toggles a class on the editor while it is down, with the link rules in `src/editor/obsidianTheme.ts` scoped to that class — verify in `tests/theme.spec.ts` that a reference shows no link cursor and no underline until the modifier is pressed, and loses both when it is released.

- [x] 8.9 Open a followed vault file with `getLeaf("tab")` rather than `getLeaf(false)` in `src/GedcomView.ts`, so the GEDCOM file keeps its place — not `"window"`, which the mobile app has no popout for. Verified by hand in 7.3: the harness has no workspace to open a tab in.
- [x] 8.10 Reveal the tab a file is already in instead of opening it again: `shownFilePath` in `src/vault/openTabs.ts` reads the path from a leaf's `getViewState()` rather than its view, because a background leaf is deferred and holds a `DeferredView`; `GedcomView.leafShowing` walks the leaves and `setActiveLeaf` brings it forward — not `revealLeaf`, which wants Obsidian 1.7.2 against a `minAppVersion` of 1.5.0 — verify in a new `src/vault/openTabs.test.ts` that a state naming a file answers its path and one naming none, or naming something that is not a path, answers nothing.

## 7. Verification

- [x] 7.1 Run `npm run check` and confirm lint, typecheck, the Vitest suites and the build all pass.
- [x] 7.2 Run `npm run test:browser` and confirm every Playwright spec passes, the new ones included.
- [ ] 7.3 Open `demo-vault/` in Obsidian and confirm by hand what the harness cannot: a real vault image under the gesture, a cropped link, the setting taking effect without reopening the file, and a Mod-click on a `FILE` payload opening the photograph in its own tab with the GEDCOM file still open behind it, a second Mod-click on the same payload bringing that tab forward rather than making another.
