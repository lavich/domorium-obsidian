## 1. Answer the two unknowns before building on them

- [x] 1.1 Spike navigation history in `demo-vault/`: a throwaway `ItemView` with `navigation = true` that holds a number in its view state and moves to the next through `leaf.setViewState`. Verify by pressing Cmd+[ and Cmd+] after three moves and recording whether the view walks back through the numbers, and whether the number survives an Obsidian restart. Write the outcome into the "Navigation rides Obsidian's history" decision in `design.md` and delete the throwaway. If Back does not walk the states, switch that decision to the fallback and adjust tasks 4.3 and 4.4 before continuing
- [x] 1.2 Spike list size in the same vault: render 1,000, 5,000 and 20,000 plain rows into a sidebar view and record the build time, the scroll feel and the heap each costs, alongside the symbol tree for the same document. Write the numbers into the "The list renders a window" decision in `design.md`. If a plain list is comfortable at 20,000 — the size the specification names — strike task 3.4 and say so in the decision

## 2. The read model, which knows nothing about Obsidian

- [x] 2.1 Create `src/genealogy/personRef.ts` with `DocumentRef { path }` and `PersonRef { document, xref }`, their equality, and their spelling to and from the subpath form `src/vault/protocolLink.ts` already uses. Verify in new `src/genealogy/personRef.test.ts`: a reference round-trips through that spelling; two references differing only by document are unequal; a path containing `#` round-trips; the document half is a value a later field can be added to without changing the call sites the other tests make
- [x] 2.2 Create `src/genealogy/names.ts` reading a `NAME` payload into a display name. Verify in new `src/genealogy/names.test.ts`: `Marie /Skłodowska-Curie/` reads as `Marie Skłodowska-Curie`; `/Curie/` reads as `Curie`; `John` with no slashes reads as `John`; an empty payload and a missing line both yield the placeholder
- [x] 2.3 Create `src/genealogy/dates.ts` reading a `DATE` payload into a `DateReading { text, year?, precision? }` by the rule in `design.md`, with no field named as the year an event happened. Verify in new `src/genealogy/dates.test.ts`: `7 NOV 1867` yields 1867 as exact; `ABT 1867` yields 1867 as approximate; `BET 1867 AND 1870` yields 1867 as a range end; `@#DHEBREW@ 5628` and `12 FEB 1867/68` yield no year; every case carries the payload through unchanged
- [x] 2.4 Create `src/genealogy/index.ts` building `GenealogyIndex` from `DocumentSymbol[]` and a revision: the person rows, the identifier lookup for `INDI` and `FAM`, and each person's family identifiers. Verify in new `src/genealogy/index.test.ts` against the fixture from task 2.7: 17 identified people in document order; a record declaring no cross-reference reported and marked unaddressable rather than dropped; rows carrying name, date readings and a place
- [x] 2.5 Add relative resolution to `src/genealogy/` — parents through `FAMC`, partners and children through `FAMS`, spouses and children read only from the family roles GEDCOM defines for each, held as a named table rather than as any pointer a family carries, with no sex check and no cardinality assumption. Each person reported once. Verify in `src/genealogy/index.test.ts`: the fixture's parents, partner and children resolve; a person is not their own partner; a shared parent of two families appears once; a family naming two people in the same spouse role reports both; a person recorded female in the husband role is reported as a spouse; a pointer to a person in a role that is neither a spouse nor a child role is reported as neither
- [x] 2.6 Add event reporting to `src/genealogy/` over a named table of the event and attribute tags GEDCOM defines for an individual record in both dialects — births, deaths, burials, christenings, baptisms, censuses, education, occupation, residence, immigration, emigration, naturalisation, retirement, graduation, probate, will and the rest of that set — each event carrying its tag, date reading, place payload and own payload, in record order. Verify in `src/genealogy/index.test.ts`: a birth with date and place; an occupation carrying its own text; a bare `1 DEAT` still reported; a `1 CENS` reported, which the first draft's seven-tag list would have dropped; `SEX`, `FAMC` and `SOUR` reported as events by none of it
- [x] 2.7 Add unresolved-reference handling, and create the GEDCOM fixture the tests above use — a small document with a full family, a person carrying only a name, a family pointing at a missing person, and a person pointing at a missing family. Verify in `src/genealogy/index.test.ts`: the missing child is reported as unresolved carrying `@I99@`, its siblings resolve normally, and the person with the missing family reads without parents
- [x] 2.8 Add the revision rule to `src/genealogy/` and its caller: the index is built for a document and a revision and reused until that revision changes, the revision for an open document being a counter the plugin increments where it already calls `GedcomLanguageService.update`, and for a closed document the `mtime:size` pair `recordIndex.ts` uses. Do not read a revision from `getVersionResolution()`, which returns the GEDCOM format version. Verify in `src/genealogy/index.test.ts`: asking twice at one revision builds once; a new revision rebuilds; and by hand in `demo-vault/` that editing a birth year without saving changes what the sidebar and an open Person view show
- [x] 2.9 Assert the module's independence: verify with `grep -rn 'from \"obsidian\"' src/genealogy` returning nothing, with `grep -rn 'from \"../i18n\"\|from \"./i18n\"' src/genealogy` returning nothing, and by a case in `src/genealogy/index.test.ts` that builds the index from document text alone and asserts an event carries its tag and no name for it

## 3. The people list in the sidebar

- [x] 3.1 Create `src/people/peopleList.ts` rendering rows from the model's person rows into a plain DOM container, with the years span and the place, no identifier leading a row, unaddressable people left out, and the count above the list. It imports nothing from `obsidian` and nothing from `src/i18n/`, taking its container, its naming function and its click handler, as `src/editor/searchPanel.ts` takes its host. Verify in new `src/people/peopleList.test.ts` (happy-dom), passing a stub naming function: a person with both years and a place; `1931–` for an unrecorded death; a name-only person carrying no empty years and no empty place; an unaddressable person absent from the list and from the count
- [x] 3.2 Add filtering to `src/people/peopleList.ts` over the searchable text built in task 2.4. Verify in `src/people/peopleList.test.ts`: `curie` matches whatever the case; `I1` and `@I1@` both match `@I1@`; a year and a place each match; text nothing matches shows the no-results message; clearing restores the list
- [x] 3.3 Create `src/people/PeopleView.ts`, the `ItemView` for the left sidebar: it follows the active `GedcomView` through `active-leaf-change`, keeps its last GEDCOM when the reader moves to a note, shows the no-GEDCOM message before one is opened, and opens a person on click. Verify by hand in `demo-vault/`: opening `curie.ged` lists 17 people; moving to a note keeps them; choosing a row opens Person view
- [x] 3.4 Add the rendering window to `src/people/peopleList.ts` — fixed row height, a spacer of the full height, rows for the viewport plus a margin — unless task 1.2 struck this task. Verify in `src/people/peopleList.test.ts` that a list of 20,000 rows puts a bounded number of row elements in the container, and by hand in `demo-vault/` that scrolling a document of 20,000 people stays smooth. Nothing is claimed above that size

## 4. The Person view

- [x] 4.1 Create `src/person/personPage.ts` rendering one person into a plain DOM container, with the signature `renderPersonPage(container, person, { t, onPersonClick, onOpenSource })`: identity, family groups, events, and the Open in GEDCOM control, with absent fields and empty groups left out. It imports nothing from `obsidian` and nothing from `src/i18n/` — the naming function is handed in, as `PanelHost` and `MediaPreviewHost` hand in theirs. Verify in new `src/person/personPage.test.ts` (happy-dom), passing a stub naming function rather than setting the plugin's language: a full person shows both dates and both places; a name-only person shows a heading and no empty rows; a person with no family shows no family groups; a person with no events shows no events section; choosing a relative calls back rather than opening anything
- [x] 4.2 Add the event keys and the section headings to `src/i18n/en.json` and `src/i18n/ru.json`, and supply `t` from the catalogue where `src/person/PersonView.ts` calls the renderer. The catalogue need not name every tag the model reads; a tag it does not name is shown as the tag. Verify in `src/i18n/i18n.test.ts` that the new keys exist on both sides with matching placeholders, and in `src/person/personPage.test.ts` that an unknown tag is labelled by the tag itself and that a Hebrew date is shown as written
- [x] 4.3 Create `src/person/PersonView.ts`, the workspace `ItemView`: `navigation = true`, the `PersonRef` as its view state, `getState`/`setState` so Obsidian persists and restores it, the display text set to the person's name, and the document read from an open `GedcomView` or through `VaultReader` where none is open. Verify by hand in `demo-vault/`: opening a person from the sidebar shows them; restarting Obsidian with the view open shows the same person
- [x] 4.4 Make relatives navigate by the route task 1.1 settled — `setViewState` into the same leaf, or the fallback trail in view state with a back control in the page. Verify by hand in `demo-vault/`: following a parent shows them in the same tab with no second tab; going back twice returns through the two people visited; going forward returns; going back from the first person leaves the view showing something; an unresolved relative opens nothing and says the record is not in this file
- [x] 4.5 Wire Open in GEDCOM in `src/person/PersonView.ts` over the existing `GedcomView.goToXref` and ephemeral-state subpath path, revealing an already-open tab rather than opening a second. Verify by hand in `demo-vault/`: the action puts the cursor on `0 @I1@ INDI`; with the file already open in another tab, that tab is revealed; with the file removed, a notice says so and nothing opens; with the file renamed while a Person view is open, the same notice appears rather than a crash

## 5. Registration, style and a second document

- [x] 5.1 Register both views in `src/main.ts`, add the command that opens the people list, reveal an existing sidebar view rather than creating a second, and restore the sidebar through `onLayoutReady`. Verify by hand in `demo-vault/`: the command opens the view; running it again with the view in a collapsed sidebar reveals that one; restarting keeps it in place
- [x] 5.2 Style the list and the page in `styles.css` on Obsidian's own variables only, with no colour literals. Verify with `grep -nE '#[0-9a-fA-F]{3,8}|rgb\(' styles.css` showing no new literals, and by hand in `demo-vault/` in both a light and a dark theme
- [x] 5.3 Prove two documents with overlapping identifiers stay apart: add a second GEDCOM to `demo-vault/` declaring its own `@I1@`, and verify by hand that opening `@I1@` from each file shows that file's person, and that a Person view restored after a restart shows the one it was opened from

## 6. Check

- [x] 6.1 Run `npm run check` and `npm run test:browser`, both green

## 7. What the first reading of the built thing asked for

- [x] 7.1 Label the identity summary in `src/person/personPage.ts`: Born, Died and Sex, each with its place beneath the date it belongs to, read from the person's own birth and death events so that both places are available rather than the single one the list row carries. A label appears only with the field it names. Verify in `src/person/personPage.test.ts`: a full record shows both labelled dates each above its own place; a birth with no place shows no empty line; a person with only a death shows no Born label
- [x] 7.2 Show the document and the identifier beneath the name in `src/person/personPage.ts`, and make the identifier the way the reader reaches the record, replacing the separate control. Add the keys to `src/i18n/en.json` and `ru.json`. Verify in `src/person/personPage.test.ts`: the file's name and `@I1@` are shown; choosing the identifier calls back for the source; two people from different documents each name their own file
- [x] 7.3 Name the document above the list in `src/people/peopleList.ts`, beside the count. Verify in `src/people/peopleList.test.ts`: the name and the count are shown together; the name follows when the document is replaced
- [x] 7.4 Make a relative's whole row read as the target it already is, in `styles.css`: the hover covers the row, the years are not dimmed away from it, and the name is not the only thing that looks chooseable. Verify by hand in `demo-vault/` that the row highlights and opens wherever it is clicked
- [x] 7.5 Tighten the sidebar rows by about a fifth and weaken the separators between the family groups in `styles.css`, keeping one before the events section. Verify by hand in `demo-vault/` in both themes, and re-check that the row height the window counts by still matches the styled height
- [x] 7.6 Run `npm run check` and `npm run test:browser`, both green

## 8. The face beside the name

- [x] 8.1 Read a person's multimedia in `src/genealogy/`: the `OBJE` links a record carries, each resolved to the file the document names — through a multimedia record or from a `FILE` beneath the link itself — with the caption and the rectangle where the record gives them, in record order, and the first the document calls an image marked as the portrait. Nothing is fetched or measured. Verify in `src/genealogy/index.test.ts`: the fixture's portrait, its rectangle and its caption; a record naming its file directly; a sound recording reported and not made the portrait; an `OBJE` pointing at a record that is not declared reported as unresolved with the person's other pictures intact
- [x] 8.2 Draw the portrait beside the name in `src/person/personPage.ts`, cut to the rectangle where there is one, bounded, reusing `drawnCrop`, `cropScale` and the crop application already written for the media preview rather than writing a second one. The host resolves a vault path to something drawable and answers nothing for a file it cannot; a picture that will not draw leaves the page as if none were named. Verify in `src/person/personPage.test.ts`: a portrait is drawn; a rectangle is applied once the image reports its size; a person with no portrait has no frame; a host that resolves nothing draws no frame; a picture at an `https` address is not requested
- [x] 8.3 Supply the resolver from `src/person/PersonView.ts` over the vault, as `GedcomView` already does for the media preview. Verify by hand in `demo-vault/`: Marie shows her portrait cut to the rectangle her record names, and a person with no picture shows none
- [x] 8.4 Style the portrait in `styles.css` on Obsidian's own variables, bounded and keeping its proportions, beside the name rather than above it. Verify by hand in `demo-vault/` in both themes
- [x] 8.5 Run `npm run check` and `npm run test:browser`, both green
- [x] 8.6 Mark the row of the person the Person view is showing, in `src/people/peopleList.ts` and `src/people/PeopleView.ts`: at most one, following whoever is shown however the reader got there, and none where the person shown is from another document. Verify in `src/people/peopleList.test.ts`: choosing marks one row and unmarks the previous; a mark for a person filtered out shows nothing; marking a person the list does not hold marks nothing
- [x] 8.7 Open the picture's own file when the reader chooses the portrait, whole rather than cut, in `src/person/personPage.ts` and `src/person/PersonView.ts`. A new tab rather than a popout window, which the mobile app does not have — the same decision `GedcomView` already records for a vault file. Verify in `src/person/personPage.test.ts` that choosing the portrait calls back with the picture, and by hand in `demo-vault/` that Frédéric's portrait opens the double photograph he was cut from
- [x] 8.8 Title the tab with the person rather than the view, in `src/person/PersonView.ts`: the header is drawn when the view is created, before any person is set, and nothing refreshes it afterwards, so it keeps saying the view's name. Refresh it once the person is drawn, reaching for the undocumented method with the same care `main.ts` already takes over the suggest registry. Verify by hand in `demo-vault/`: following a relative retitles both the tab and the header
- [x] 8.9 Make the document's name in the source line open the document, in `src/person/personPage.ts` and `src/person/PersonView.ts`, as its identifier already opens the record. Verify in `src/person/personPage.test.ts`: choosing the name calls back for the document, and choosing the identifier still calls back for the record
