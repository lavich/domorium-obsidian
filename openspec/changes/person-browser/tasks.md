## 1. Answer the two unknowns before building on them

- [ ] 1.1 Spike navigation history in `demo-vault/`: a throwaway `ItemView` with `navigation = true` that holds a number in its view state and moves to the next through `leaf.setViewState`. Verify by pressing Cmd+[ and Cmd+] after three moves and recording whether the view walks back through the numbers, and whether the number survives an Obsidian restart. Write the outcome into the "Navigation rides Obsidian's history" decision in `design.md` and delete the throwaway. If Back does not walk the states, switch that decision to the fallback and adjust tasks 4.3 and 4.4 before continuing
- [ ] 1.2 Spike list size in the same vault: render 1,000, 5,000 and 20,000 plain rows into a sidebar view and record the build time, the scroll feel and the heap each costs, alongside the symbol tree for the same document. Write the numbers into the "The list renders a window" decision in `design.md`. If a plain list is comfortable at the sizes a real document reaches, strike task 3.4 and say so in the decision

## 2. The read model, which knows nothing about Obsidian

- [ ] 2.1 Create `src/genealogy/personRef.ts` with the `{ file, xref }` pair, its equality, and its spelling to and from the subpath form `src/vault/protocolLink.ts` already uses. Verify in new `src/genealogy/personRef.test.ts`: a pair round-trips through that spelling; two pairs differing only by file are unequal; a path containing `#` round-trips
- [ ] 2.2 Create `src/genealogy/names.ts` reading a `NAME` payload into a display name. Verify in new `src/genealogy/names.test.ts`: `Marie /Skłodowska-Curie/` reads as `Marie Skłodowska-Curie`; `/Curie/` reads as `Curie`; `John` with no slashes reads as `John`; an empty payload and a missing line both yield the placeholder
- [ ] 2.3 Create `src/genealogy/dates.ts` reading a year from a `DATE` payload by the rule in `design.md`. Verify in new `src/genealogy/dates.test.ts`: `7 NOV 1867`, `ABT 1867` and `BET 1867 AND 1870` all yield 1867; `@#DHEBREW@ 5628` and `12 FEB 1867/68` yield no year; every case carries the payload through unchanged
- [ ] 2.4 Create `src/genealogy/index.ts` building `GenealogyIndex` from `DocumentSymbol[]`: the person rows, the identifier lookup for `INDI` and `FAM`, and each person's family identifiers. Verify in new `src/genealogy/index.test.ts` against the fixture from task 2.7: 17 people in document order, a record with no identifier omitted, rows carrying name, years and a place
- [ ] 2.5 Add relative resolution to `src/genealogy/` — parents through `FAMC`, partners and children through `FAMS`, spouses read from whichever tags a family uses, each person reported once. Verify in `src/genealogy/index.test.ts`: the fixture's parents, partner and children resolve; a person is not their own partner; a shared parent of two families appears once; a family with two spouses under one tag reports both
- [ ] 2.6 Add event reporting to `src/genealogy/` for `BIRT`, `DEAT`, `BURI`, `RESI`, `OCCU`, `IMMI`, `EMIG`, each carrying its tag, date payload, place payload and own payload, in record order. Verify in `src/genealogy/index.test.ts`: a birth with date and place; an occupation carrying its own text; a bare `1 DEAT` still reported
- [ ] 2.7 Add unresolved-reference handling, and create the GEDCOM fixture the tests above use — a small document with a full family, a person carrying only a name, a family pointing at a missing person, and a person pointing at a missing family. Verify in `src/genealogy/index.test.ts`: the missing child is reported as unresolved carrying `@I99@`, its siblings resolve normally, and the person with the missing family reads without parents
- [ ] 2.8 Assert the module's independence: verify with `grep -rn 'from "obsidian"' src/genealogy` returning nothing, and by a case in `src/genealogy/index.test.ts` that builds the index from document text alone

## 3. The people list in the sidebar

- [ ] 3.1 Create `src/people/peopleList.ts` rendering rows from the model's person rows into a plain DOM container, with the years span and the place, no identifier leading a row, and the count above the list. It imports nothing from `obsidian`, taking its container and its click handler, as `src/editor/searchPanel.ts` does. Verify in new `src/people/peopleList.test.ts` (happy-dom): a person with both years and a place; `1931–` for an unrecorded death; a name-only person carrying no empty years and no empty place
- [ ] 3.2 Add filtering to `src/people/peopleList.ts` over the searchable text built in task 2.4. Verify in `src/people/peopleList.test.ts`: `curie` matches whatever the case; `I1` and `@I1@` both match `@I1@`; a year and a place each match; text nothing matches shows the no-results message; clearing restores the list
- [ ] 3.3 Create `src/people/PeopleView.ts`, the `ItemView` for the left sidebar: it follows the active `GedcomView` through `active-leaf-change`, keeps its last GEDCOM when the reader moves to a note, shows the no-GEDCOM message before one is opened, and opens a person on click. Verify by hand in `demo-vault/`: opening `curie.ged` lists 17 people; moving to a note keeps them; choosing a row opens Person view
- [ ] 3.4 Add the rendering window to `src/people/peopleList.ts` — fixed row height, a spacer of the full height, rows for the viewport plus a margin — unless task 1.2 struck this task. Verify in `src/people/peopleList.test.ts` that a list of 20,000 rows puts a bounded number of row elements in the container, and by hand in `demo-vault/` that scrolling a large document stays smooth

## 4. The Person view

- [ ] 4.1 Create `src/person/personPage.ts` rendering one person into a plain DOM container: identity, family groups, events, and the Open in GEDCOM control, with absent fields and empty groups left out. It imports nothing from `obsidian`. Verify in new `src/person/personPage.test.ts` (happy-dom): a full person shows both dates and both places; a name-only person shows a heading and no empty rows; a person with no family shows no family groups; a person with no events shows no events section
- [ ] 4.2 Add event naming to `src/person/personPage.ts` through `t`, and add the seven event keys and the section headings to `src/i18n/en.json` and `src/i18n/ru.json`. Verify in `src/i18n/i18n.test.ts` that the new keys exist on both sides with matching placeholders, and in `src/person/personPage.test.ts` that an unknown tag is labelled by the tag itself and that a Hebrew date is shown as written
- [ ] 4.3 Create `src/person/PersonView.ts`, the workspace `ItemView`: `navigation = true`, the `PersonRef` as its view state, `getState`/`setState` so Obsidian persists and restores it, the display text set to the person's name, and the document read from an open `GedcomView` or through `VaultReader` where none is open. Verify by hand in `demo-vault/`: opening a person from the sidebar shows them; restarting Obsidian with the view open shows the same person
- [ ] 4.4 Make relatives navigate by the route task 1.1 settled — `setViewState` into the same leaf, or the fallback trail in view state. Verify by hand in `demo-vault/`: following a parent shows them in the same tab with no second tab; Back twice returns through the two people visited; Forward returns; an unresolved relative opens nothing and says the record is not in this file
- [ ] 4.5 Wire Open in GEDCOM in `src/person/PersonView.ts` over the existing `GedcomView.goToXref` and ephemeral-state subpath path, revealing an already-open tab rather than opening a second. Verify by hand in `demo-vault/`: the action puts the cursor on `0 @I1@ INDI`; with the file already open in another tab, that tab is revealed; with the file removed, a notice says so and nothing opens

## 5. Registration, style and a second document

- [ ] 5.1 Register both views in `src/main.ts`, add the command that opens the people list, reveal an existing sidebar view rather than creating a second, and restore the sidebar through `onLayoutReady`. Verify by hand in `demo-vault/`: the command opens the view; running it again with the view in a collapsed sidebar reveals that one; restarting keeps it in place
- [ ] 5.2 Style the list and the page in `styles.css` on Obsidian's own variables only, with no colour literals. Verify with `grep -nE '#[0-9a-fA-F]{3,8}|rgb\(' styles.css` showing no new literals, and by hand in `demo-vault/` in both a light and a dark theme
- [ ] 5.3 Prove two documents with overlapping identifiers stay apart: add a second GEDCOM to `demo-vault/` declaring its own `@I1@`, and verify by hand that opening `@I1@` from each file shows that file's person, and that a Person view restored after a restart shows the one it was opened from

## 6. Check

- [ ] 6.1 Run `npm run check` and `npm run test:browser`, both green
