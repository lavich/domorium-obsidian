## 1. Confirm the drag survives a real popover

- [ ] 1.1 In `demo-vault`, open a `.ged` with a cropped `OBJE` link, open the preview, and hold the primary button down on the picture for several seconds while moving the pointer off the popover and across the editor. Verified by the observation itself: if Obsidian's `HoverPopover` closes mid-drag, stop and say so rather than building the rest on top — the harness spike says it should not, but the harness stubs `HoverPopover`
- [ ] 1.2 In the same session, confirm the picture does not start a native image drag once `dragstart` is prevented, and that a wheel over the popover does not scroll the note behind it. Verified by observation; both were measured in the harness and both need the real popover's chrome around them

## 2. The geometry, as numbers

- [ ] 2.1 Add to `src/editor/media.ts`: `MediaView = { scale; tx; ty }`, `MediaGeometry = { naturalWidth; naturalHeight; bounds }` — the one value the rest of these functions take — `MEDIA_PREVIEW_MAX_MAGNIFY = 4`, and `scaleLimits(geometry) -> { min, max }` with `min = Math.min(1, fit(naturalWidth, naturalHeight, bounds))` and `max` the constant. Verify in `src/editor/media.test.ts`: a photograph larger than the bound gives `min` below 1 and `max` 4; a photograph the bound would magnify gives `min` exactly 1, not the 3.5× fitting it would ask for, so the range stays `[1, 4]` and the two limits can never cross; a pane of zero falls back to the ceiling bound as `previewBounds` already does
- [ ] 2.2 Add `clampPan(view, geometry) -> MediaView`, clamping each axis independently: cover the viewport where the scaled photograph is at least as large, centre it where it is smaller. Verify in `src/editor/media.test.ts`: a drag past each of the four edges stops at it, a photograph smaller than the viewport is centred whatever `tx`/`ty` asked for, and at `scaleLimits(...).min` both axes are pinned — the axis `fit` was taken from because it is covered exactly, the other because it is centred
- [ ] 2.3 Add `cropView(crop, geometry) -> MediaView`, fitting the (already clamped) rectangle into the viewport, centring it, and putting the result through `clampPan`. Verify in `src/editor/media.test.ts`: a rectangle larger than the bound scales down as `cropScale` did, a rectangle smaller than it magnifies — the case `cropScale` capped at 1 — a tiny rectangle stops at 4×, and the rectangle is **wholly inside the viewport** for a rectangle against each of the four edges of the photograph, where centring it would leave the viewport short of picture and the pan clamp moves it
- [ ] 2.4 Add `zoomTo(view, factor, px, py, geometry) -> MediaView`, keeping the image point under `px`/`py` fixed, clamping the scale to `scaleLimits` and the result through `clampPan`. Verify in `src/editor/media.test.ts`: the point under the pointer is unmoved, a factor beyond either limit lands exactly on it, and a repeated zoom out ends at the whole photograph centred — at its own pixels where the viewport is the larger
- [ ] 2.5 Delete `cropScale` and its five cases from `src/editor/media.test.ts`, and confirm nothing that ships imports it (`grep -rn cropScale src harness tests`, whose only other hit is the untracked `harness/dist/`). Verify `npm run test` and `npm run lint` are clean; `drawnCrop` and `previewBounds` keep their tests unchanged

## 3. The viewport

- [ ] 3.1 In `src/editor/mediaPreviewView.ts`, replace `applyCrop` with `applyView(frame, image, view)` that writes **only** the image's `transform`. The frame's size is not written from TypeScript at all: `.gedcom-media-cropped` takes it from `--gedcom-media-max-w`/`--gedcom-media-max-h`, which `renderMediaPreview` already sets on the root before the image loads. Verify in `src/editor/mediaPreviewView.test.ts` that a cropped frame carries the class and no inline width or height for two rectangles of different sizes, and that the frame is not given `display: flex`; the box itself is measured in `tests/mediaPreview.spec.ts`, happy-dom applying no stylesheet
- [ ] 3.2 Compute the view in the `load` handler from `drawnCrop` and `cropView`, behind the existing `host.isCurrent()` guard, and keep the existing `error` path untouched. The view is the state: absent until `load`, and every handler and the button read it. Verify in `src/editor/mediaPreviewView.test.ts` by firing `load` with a stubbed `naturalWidth`/`naturalHeight`: the transform matches `cropView`'s numbers, and a `load` fired after `isCurrent()` goes false writes nothing
- [ ] 3.3 Give `.gedcom-media-cropped` the viewport's width and height in `styles.css` from those two properties, beside where they are read today, and add `touch-action: none` and `user-select: none` to it. Verify in `tests/mediaPreview.spec.ts` that the frame's box is the bound in both themes and that a cropped popover is not cut off at its edge, which the bounding specs already assert

## 4. The drag and the wheel

- [ ] 4.1 Install `pointerdown`/`pointermove`/`pointerup`/`pointercancel` on the frame for a cropped reference only, taking `setPointerCapture` on down and applying the delta through `clampPan`, every handler behind `host.isCurrent()` and a view that exists, and every listener on the frame so the popover's removal ends them. Verify in `src/editor/mediaPreviewView.test.ts` that the handlers are installed for a cropped reference and not for an uncropped one, that a synthesised drag moves the transform by the delta, and that the same drag before `load` writes no transform at all
- [ ] 4.2 Prevent `dragstart` on the frame, or the native image drag takes the gesture on the first movement — measured in the spike, where exactly two `pointermove` events arrived and then none. Verify in `src/editor/mediaPreviewView.test.ts` that a dispatched `dragstart` is defaultPrevented
- [ ] 4.3 Add the non-passive `wheel` listener calling `preventDefault` and `zoomTo` with the pointer's position in the frame's coordinates, with the per-event factor and its clamp. Verify in `src/editor/mediaPreviewView.test.ts`: the event is defaultPrevented whether or not there is a view yet, the scale moves in the right direction, and a run of large events stops at each limit rather than passing it

## 5. The button

- [ ] 5.1 Draw a single button beside the picture for a cropped reference, following `drawOffer`'s shape, reading **Show the whole photograph** while the region was last asked for and **Show the region** while the whole photograph was. It is drawn with the frame rather than on `load`, so the popover does not gain a row and change shape a second time, and it is `disabled` until the view exists. Taking it recomputes one of the two views rather than restoring a remembered one: `cropView(...)` for the region, and for the whole photograph `scaleLimits(...).min` centred, so that the button's state and the end of the wheel's travel are one view. Verify in `src/editor/mediaPreviewView.test.ts`: the label after each press, the view returning to `cropView`'s numbers after a drag and two presses, the button disabled before `load`, and no button at all for an uncropped reference or for a rectangle `drawnCrop` rejects
- [ ] 5.2 Style the button's row in `styles.css` from Obsidian's own variables, as the remote-images offer is styled. Verify in `tests/mediaPreview.spec.ts` in both themes, beside the existing offer-row check
- [ ] 5.3 On the `error` path, where the frame is replaced by a row, remove the button with it and leave the view absent. Verify in `src/editor/mediaPreviewView.test.ts` that a cropped image failing to load leaves the note and no button, and that a pointer gesture afterwards does nothing

## 6. The rectangle that misses

- [ ] 6.1 Where `drawnCrop` returns `null`, keep showing the whole image and add a `gedcom-media-note` reading **Rectangle outside the image** above the picture, and render the rest exactly as the uncropped case: the crop class dropped so the picture area is the image's own size again, no handlers, no button. This is the one preview that does change shape on `load`, and the reason is in `design.md`. Verify in `src/editor/mediaPreviewView.test.ts` that the note is present, the crop class is gone, and neither the button nor the pointer handlers are left installed
- [ ] 6.2 Leave a rectangle that merely overhangs an edge without a note, as decided in `design.md`. Verify in `src/editor/mediaPreviewView.test.ts` that a clamped-but-overlapping rectangle draws no note

## 7. The fixtures the browser specs read

- [ ] 7.1 Point the crop fixtures at a photograph the new specs can measure. `media/family.jpg` maps to `photo` — a flat 120×80 field with no region to tell from its surroundings, and small enough that the whole zoom range is 3.5 to 4. Map it instead to one of the `painted` images `harness/mount.ts` already builds (`target`, 600×400, has a marked region), and move the `CROP` values in `tests/harness.ts` to suit: the first rectangle inside the marked region, the second overhanging an edge of the *new* size, the third still missing the image entirely. The offsets in `tests/mediaPreview.spec.ts` are keyed by those values and move with them. Verify by sampling two known points and finding different colours, and by `npm run test:browser` reaching the assertions it used to
- [ ] 7.2 Rewrite the seven assertions written against a frame the size of the rectangle, each saying now that the rectangle is contained in and centred within what is on screen rather than equal to it: the 40-wide frame at `tests/mediaPreview.spec.ts:387`, the 20×40 overhanging one at `:442`, the silent fall-back at `:446` (see 7.6), `shownRegion` equal to the rectangle with `scale: 1` at `:800`, the four frame corners all inside the region at `:811`, the exact `shownRegion` of a rectangle larger than the bound at `:861`, and the same for one wider than the popover at `:1004`. Verify `npm run test:browser` passes, that the corner-sampling spec still fails if the image is stretched by a flex frame, and that none of the seven passes vacuously
- [ ] 7.3 Add a spec for the drag: press on the picture, move off the popover, hold for two seconds, and assert the popover is still open and the region on screen has moved. Verify in `tests/mediaPreview.spec.ts`, and include the no-capture control case as a guard that the assertion can fail
- [ ] 7.4 Add specs for the wheel — the scale changes and the page behind does not scroll — and for both limits: zooming out to the whole photograph, and in to 4×. Verify in `tests/mediaPreview.spec.ts` by `shownRegion`, the whole photograph being `0,0` to the fixture's own size
- [ ] 7.5 Add specs for the button: the whole photograph in one press, the rectangle back in two, and the label naming what the next press will do. Verify in `tests/mediaPreview.spec.ts`
- [ ] 7.6 Add a spec for the rectangle that misses the image, replacing the fall-back-silently assertion at `:446`: the note is shown, the whole photograph is drawn at its own size rather than in a viewport-sized box, and a preview with no `CROP` beside it carries no note. Verify in `tests/mediaPreview.spec.ts`
- [ ] 7.7 Add a spec that two rectangles of different sizes give picture areas of the same size, and that the area does not change when the image loads. Verify in `tests/mediaPreview.spec.ts` with the harness's held-image option

## 8. Say what the plugin does

- [ ] 8.1 Rewrite the two places `README.md` describes the cropped preview — the feature list, "cropped to the part of the image a multimedia link asks for", and the paragraph promising "the part of that photograph the link's `CROP` asks for" — as the viewport this change makes: the link opens on its rectangle, the photograph around it is there, and the reader can move it or take the button to the whole photograph. Verified by reading it beside the popover

## 9. Check

- [ ] 9.1 Run `npm run check` and `npm run test:browser`
